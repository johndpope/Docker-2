---
version: "3.7"
services:
  wikijs:
    image: linuxserver/wikijs
    container_name: wikijs
    networks:
     - traefik-public
    ports:
      - 4010:3000
    labels:
      - traefik.enable=true
      - traefik.constraint-label=traefik-public
      - traefik.docker.network=traefik-public
      - traefik.http.routers.wikijs-http.entrypoints=http
      - traefik.http.routers.wikijs-http.rule=Host(`wikijs.int.whitehome.io`)
      - traefik.http.routers.wikijs-https.entrypoints=https
      - traefik.http.routers.wikijs-https.rule=Host(`wikijs.int.whitehome.io`)
      - traefik.http.routers.wikijs-https.tls=true
      - traefik.http.routers.wikijs-http.middlewares=wikijs-https@docker
      - traefik.http.middlewares.wikijs-https.redirectscheme.scheme=https
      - traefik.http.middlewares.wikijs-https.redirectScheme.permanent=true
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    volumes:
      - /docker-data/wikijs:/config
      - /docker-data/wikijs/data:/data
    restart: unless-stopped

networks:
  traefik-public:
    external: true
